<!DOCTYPE html>
<html lang="ru">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>Оплата заказа</title>
		<link rel="stylesheet" href="/static/styles/style.css" />
	</head>
	<body>
		<main class="main">
			<div class="box">
				<button class="down" id="toggle-info">
					<span class="text"
						><%= Number(gateway.amount).toLocaleString('ru-RU') %> ₽</span
					>
					<img src="/static/icons/down.svg" class="arrow" />
				</button>
				<div class="info-wrapper">
					<div class="info">
						<div class="info-inner">
							<div class="rows">
								<div class="row">
									<span class="bold">Магазин</span>

									<a href="https://<%= gateway.domain %>"> <%= gateway.domain %> </a>
								</div>
								<% if (gateway.description) { %>
								<div class="row">
									<span class="bold">Описание</span>
									<span><%= gateway.description %></span>
								</div>
								<% } %>
							</div>
						</div>
					</div>
				</div>
			</div>

			<div class="box">
				<p class="title">Быстрая оплата</p>
				<form id="pay-form" action="/gateway/pay" method="post">
					<input type="hidden" name="projectId" value="<%= gateway.projectId %>" />
					<input type="hidden" name="orderId" value="<%= gateway.orderId %>" />
					<input type="hidden" name="amount" value="<%= gateway.amount %>" />
					<input type="hidden" name="description" value="<%= gateway.description %>" />
					<input type="hidden" name="domain" value="<%= gateway.domain %>" />
					<div class="methods">
						<% methods.forEach(function(method) { %>
						<button
							type="submit"
							class="btn method"
							name="methodId"
							value="<%= method.id %>"
						>
							<% if (method.imageSrc) { %>
							<img class="method-icon" src="<%= method.imageSrc %>" alt="method icon" />
							<% } %> <% if (method.showLabel) { %>
							<span><%= method.label %></span>
							<% } %>
						</button>
						<% }) %>
					</div>
				</form>
				<p class="terms">
					Нажимая «Оплатить», вы соглашаетесь <a href="/terms">с условиями</a>
				</p>
			</div>
			<div class="flex justify-center items-center terms">
				Не получилось оплатить?
				<a href="https://t.me/<%= supportId %>"> Свяжитесь с нами</a>
			</div>
			<div class="support">
				<a class="flex items-center gap-2" href="https://t.me/<%= supportId %>">
					<img
						width="60"
						height="60"
						src="https://upload.wikimedia.org/wikipedia/commons/thumb/8/83/Telegram_2019_Logo.svg/1024px-Telegram_2019_Logo.svg.png"
					/>
					<img
						src="https://www.svgrepo.com/show/192522/customer-service-support.svg"
						width="60"
						height="60"
					/>
				</a>
			</div>
		</main>

		<script src="/static/app.js"></script>
	</body>
</html>
