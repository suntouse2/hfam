<!DOCTYPE html>
<html lang="ru">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>Оплата заказа</title>
		<link rel="stylesheet" href="/static/styles/style.css" />
	</head>
	<body>
		<main class="main">
			<div class="box">
				<button class="down" id="toggle-info">
					<span class="text"><%= gateway.amount %> ₽</span>
					<img src="/static/icons/down.svg" class="arrow" />
				</button>
				<div class="info-wrapper">
					<div class="info">
						<div class="info-inner">
							<div class="rows">
								<div class="row">
									<span class="bold">Магазин</span>

									<a href="https://<%= gateway.domain %>"> <%= gateway.domain %> </a>
								</div>
								<% if (gateway.description) { %>
								<div class="row">
									<span class="bold">Описание</span>
									<span><%= gateway.description %></span>
								</div>
								<% } %>
							</div>
						</div>
					</div>
				</div>
			</div>

			<div class="box">
				<p class="title">Быстрая оплата</p>
				<div class="btns">
					<% project.methods.filter(m => m.active).forEach(function(method) { %>
					<button class="btn method" data-id="<%= method.id %>">
						<% if (method.imageSrc) { %>
						<img src="<%= method.imageSrc %>" alt="method icon" />
						<% } %>
						<span><%= method.label %></span>
					</button>
					<% }) %>
				</div>
				<p class="terms">
					Нажимая «Оплатить», вы соглашаетесь <a href="/terms">с условиями</a>
				</p>
			</div>
		</main>

		<script src="/static/app.js"></script>
	</body>
</html>
